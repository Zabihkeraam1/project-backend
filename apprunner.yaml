# version: 1.0
# runtime: python311

# build:
#   commands:
#     pre-build:
#       - echo "✅ Pre-build step started"
#     build:
#       - mkdir -p /app
#       - cp -r ./backend/* /app
#       - pip3 install --upgrade pip
#       - pip3 install -r /app/requirements.txt
#     post-build:
#       - echo "✅ Post-build step complete"
# run:
#   runtime-version: 3.11
#   command: python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080
#   network:
#     port: 8080


# version: 1.0
# runtime: python311

# build:
#   commands:
#     pre-build:
#       - echo "🚀 Starting build process..."
#     build:
#       - mkdir -p /app/.python-packages
#       - cp -r ./backend/* /app
#       - pip3 install --upgrade pip
#       - pip3 install --target=/usr/local/lib/python3.11/site-packages uvicorn fastapi
#       - pip3 install --target=/usr/local/lib/python3.11/site-packages -r /app/requirements.txt
#       # Verification
#       - python3 -c "import uvicorn; print(f'✅ Uvicorn {uvicorn.__version__} verified')"
#     post-build:
#       - echo "📦 Build completed successfully"

# run:
#   command: python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080
#   network:
#     port: 8080

version: 1.0
runtime: python311

build:
  commands:
    pre-build:
      - echo "🚀 Starting build process..."
    build:
      # System-wide installation (recommended by AWS)
      - python3.11 -m pip install --upgrade pip
      - python3.11 -m pip install --no-warn-script-location uvicorn fastapi
      - python3.11 -m pip install --no-warn-script-location -r ./backend/requirements.txt
      # Verification
      - python3.11 -c "import uvicorn; print(f'✅ Uvicorn {uvicorn.__version__} at {uvicorn.__file__}')"
    post-build:
      - echo "📦 Build completed successfully"

run:
  command: python3.11 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
